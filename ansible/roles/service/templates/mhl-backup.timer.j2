[Unit]
Description=McHomeLab backup timer for {{ item.key }} ({{ ansible_managed }})
[Timer]
OnCalendar={{ item.value.backup.schedule | default('daily') }}
Persistent=true
RandomizedDelaySec=15m
[Install]
WantedBy=timers.target
